name: CI/CD — Docker build, push and deploy

on:
  push:
    branches: [ master ]
  workflow_dispatch:

env:
  IMAGE_NAME: ai_invoicing

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Restore pip cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: pip-${{ runner.os }}-

      - name: Build image (with build cache)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: false
          load: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Save image to tarball
        run: |
          IMAGE=${{ env.IMAGE_NAME }}:latest
          docker save "$IMAGE" -o image.tar

      - name: Ensure tarball is readable
        run: |
          ls -l image.tar || true
          chmod 644 image.tar || true

      - name: Copy image tarball to remote via SCP
        uses: appleboy/scp-action@v0.1.5
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          source: "image.tar"
          target: "/tmp/image.tar"
          timeout: 600s

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy on remote
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          timeout: 600s
          script: |
            set -e
            # Load image transferred by SCP and restart services
            if [ -f /tmp/image.tar ]; then
              docker load -i /tmp/image.tar
            elif [ -d /tmp/image.tar ]; then
              TARFILE=$(find /tmp/image.tar -type f -iname '*.tar' -print -quit)
              if [ -n "$TARFILE" ]; then
                docker load -i "$TARFILE"
              else
                TARFILE=$(find /tmp/image.tar -type f -print -quit)
                if [ -n "$TARFILE" ]; then
                  docker load -i "$TARFILE"
                else
                  echo "No tar file found in /tmp/image.tar" >&2
                  exit 1
                fi
              fi
            else
              echo "/tmp/image.tar not found" >&2
              exit 1
            fi
            rm -rf /tmp/image.tar
            cd /root/tools/ai_invoicing
            docker compose up -d --remove-orphans
