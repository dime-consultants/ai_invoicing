version: "3.9"

x-django-env: &django-env
  DJANGO_DEBUG: false
  DJANGO_SETTINGS_MODULE: ai_invoicing.settings
  DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY}

  DATABASE_DB: ${DATABASE_DB}
  DATABASE_USER: ${DATABASE_USER}
  DATABASE_PASSWORD: ${DATABASE_PASSWORD}
  DATABASE_HOST: ${DATABASE_HOST}
  DATABASE_PORT: ${DATABASE_PORT}

  CELERY_BROKER_URL: ${CELERY_BROKER_URL}

  SYSTEM_BASE_URL: invoicing.dimeconsultants.africa

x-app-image: &app-image ${IMAGE_TAG}

x-common-labels: &common-labels
  observability.app: ai-invoicing
  observability.environment: ${SHARED_ENV}

networks:
  default:
    name: ${SHARED_NETWORK}
    external: true

services:
  invoicing:
    image: *app-image
    container_name: ${SHARED_ENV}_invoicing_app
    labels:
      <<: *common-labels
      observability.component: api
    environment:
      <<: *django-env
    ports:
      - "${API_PORT}:8000"
    restart: always

  celery_worker:
    image: *app-image
    container_name: ${SHARED_ENV}_invoicing_celery_worker
    labels:
      <<: *common-labels
      observability.component: celery-worker
    command: celery -A notify worker -l INFO
    environment:
      <<: *django-env
    restart: always

  celery_beat:
    image: *app-image
    container_name: ${SHARED_ENV}_invoicing_celery_beat
    labels:
      <<: *common-labels
      observability.component: celery-beat
    command: celery -A ai_invoicing beat -l INFO
    environment:
      <<: *django-env
    restart: always
